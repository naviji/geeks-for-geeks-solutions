
    // while (l <=r ) {
    //     if (arr[l] > pivot && arr[r] < pivot) {
    //         swap(&arr[l++], &arr[r--]);
    //     }
    //     if (arr[l] <= pivot) {
    //         l++;
    //     }
    //     if (arr[r] >= pivot) {
    //         r--;
    //     }
    // }
    // swap(&arr[left], &arr[l]);
    // return r;